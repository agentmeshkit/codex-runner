# @agentmeshkit/codex-runner

Node runtime wrapper for `codex exec` and `codex exec resume`.

This package will own process spawning, JSONL normalization, cancellation,
timeouts, and stream events for Codex-backed agent applications.

## Status

Planning. See [docs/PRD.md](docs/PRD.md).

